<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.iko.android.weather.ui.main.favorites.FavoritesViewModel"/>
    </data>

    <FrameLayout style="@style/view_fill_parent">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/rv_favorites"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:listitem="@layout/item_favorite_country"
            app:adapterFavs="@{viewModel.forecastList}"/>

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/fab_add_country"
            style="@style/float_button"
            app:srcCompat="@drawable/ic_add"
            android:onClick="@{() -> viewModel.onAddCountryClick()}"/>

    </FrameLayout>

</layout>