<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.iko.android.weather.ui.splash.SplashViewModel"/>
    </data>

    <FrameLayout
        style="@style/view_fill_parent"
        android:background="@drawable/splash_bgr_sky">

        <ImageView
            android:id="@+id/iv_sun"
            style="@style/image_splash"
            android:scaleType="fitXY"
            app:srcCompat="@drawable/animated_splash_logo"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_marginBottom="@dimen/margin_small"
            android:gravity="center"
            android:text="@string/splash_app_title"/>

    </FrameLayout>

</layout>
